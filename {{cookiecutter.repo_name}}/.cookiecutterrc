# This file exists so you can easily regenerate your project.
#
# `cookiepatcher` is a convenient shim around `cookiecutter`
# for regenerating projects. To use it:
#
#    pip install cookiepatcher
#    cookiepatcher gh:ionelmc/cookiecutter-pylibrary project-to-regenerate
#
# See:
#    https://pypi.python.org/pypi/cookiecutter
#
# Alternatively, you can copy this file to ~/.cookiecutterrc
# and use `cookiecutter --overwrite-if-exists`


default_context:
{% for key, value in cookiecutter.items()|sort %}
    {{ "{0:26}".format(key + ":") }} {{ "{0!r}".format(value).strip("u") }}
{%- endfor %}
